[package]
name = "weighted-prior-research"
version = "0.9.3"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
alloy-primitives = { version = "1", features = ["serde"] }
serde = { version = "1.0.219", features = ["derive"] }
sha2 = { version = "0.10", default-features = false }
hex = "0.4"

[dev-dependencies]
serde_json = "1.0.140"

# Research-only and deliberately detached from the production root workspace.
[workspace]
