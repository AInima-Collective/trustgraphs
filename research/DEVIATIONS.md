# Deviations

Build-time deviations from the normative plans — the milestone build plans (platform reorg,
offchain attestations, hypercerts, contributions, the repo cleanup, and the proof scheduler) plus
[`research/OFFCHAIN_ATTESTATIONS_ZK.md`](./OFFCHAIN_ATTESTATIONS_ZK.md),
[`research/MULTI_PROGRAM_PLATFORM.md`](./MULTI_PROGRAM_PLATFORM.md), and
[`research/HYPERCERTS_ATPROTO_PLAN.md`](./HYPERCERTS_ATPROTO_PLAN.md): what changed, why,
and which plan section it touches. No silent divergence. Convention: the active build plan lives at
the repo root as `GOAL.md` while a build is in flight and is deleted (not archived) when done —
history lives in git and in this file. "GOAL.md" in the table below refers to whichever build plan
was active on that row's date, and milestone codes (M0, M1, …) are that plan's own build-stage
labels.

| # | Date | Plan section | What | Why |
|---|---|---|---|---|
| 1 | 2026-07-14 | GOAL.md M0 exit ("both existing programs prove e2e on anvil through the new CLI") | The M0 e2e (`test/e2e/run.sh`, on-chain stage) proves both programs via the new CLI with `SP1_PROVER=mock` and submits through the **real** `SP1JournalVerifier` → `MerkleSnapshot.submitProof` / `SignerSyncZkModule.submitSignerProof` paths, with the SNARK check stubbed at a `MockSP1Gateway` at the `ISP1Verifier` seam. Groth16-real proving was NOT run at M0 exit. | The build box has ~11 GiB RAM (local Groth16 needs 16–32 GiB per LOCAL_TESTING.md) and no prover-network key. Everything M0 changed — CLI, journal binding, vkey pinning, proof-blob encode/decode, contract wiring — is exercised; the SNARK itself is unchanged v1 SP1 machinery. A real-proof e2e rides on M1's live prover-network request (GOAL M1) and M2's rehearsed deploy. |
| 2 | 2026-07-14 | HYPERCERTS_ATPROTO_PLAN §2 (collection table) / §4 | The real `@hypercerts-org/lexicon` v1.1.0 shapes differ from the plan's table: `link.evm` is nested (`proof.message` + `proof.signature`, top-level `address`; key type `any`), `evaluation.subject` and `.score` are OPTIONAL (required: evaluators/summary/createdAt), DID references are `{did:"…"}` wrapper objects, `activity.contributors[].contributorIdentity` is a union with the DID at `.identity`, and `contributors`/`contributionWeight`/`badge.response.weight` are all optional. EIP-712 domain for `link.evm` lives in the lexicon's tests, not the schema JSON (`IdentityLink` v1, no verifyingContract/salt). | Measured against the pinned lexicon at M1 (research/offchain/05-spike-results.md §2). M4's `hypercerts-core` decode + E3/E4 skip rules must follow the real shapes; the plan doc's table stands as written history, this entry is the correction of record. Also for the partner brief: a stock PDS rejects `validate:true` for these NSIDs — records land unvalidated, so guest-side deterministic skip rules (§3.5) are the ONLY shape enforcement. |
| 3 | 2026-07-14 | OFFCHAIN_ATTESTATIONS_ZK §4.2 (envelope 0: "subtract the on-chain revokeOffchain deletion set — chain-complete for free") | Envelope-0 revocation is **in-log**: the chained log carries typed entries (attest/revoke) and the deletion set's completeness is inherited from the signed head. The on-chain `EAS.revokeOffchain` channel is NOT consumed by the guest in v1. | There is no sound way to bind the on-chain deletion set into the proven statement as designed: the guest cannot read chain state, the journal is frozen at 10 fields, and an unbound witness-supplied deletion set would let a prover include revoked edges (a soundness hole, not a liveness one). The two mechanisms that would bind it — an Ethereum storage-proof witness against a checkpointed block hash, or a permissionless on-chain revocation-mirror folded into an accumulator — are both deliberate future events (journal/checkpoint change or new contract). In-log revocation matches the atproto envelope's revocation-by-absence and costs attesters nothing (append one entry, re-sign the head). |
| 4 | 2026-07-23 | GOAL.md (contributions) Interface freeze — Params layout | The contributions `paramsHash` includes `trustDecayFp` (slot 8), though GOAL.md's IF shorthand list of mirrored rep params omits it. 21 fields total, frozen in `docs/build/contributions/interfaces.md` §3. | Stage-1 rep is `pagerank-core`'s algorithm unmodified ("import, don't fork"), and `RankConfig` requires a decay value. A pinned param beats a hardcoded constant; omitting it would make stage-1 a different algorithm than the trust program's. CONTRIBUTION_FUNDING.md §4 ("PageRank params") is satisfied either way. |
| 6 | 2026-07-24 | GOAL.md (cleanup) M2 — Localism Fund removal | The indexer's offchain drizzle migrations were **squashed to a fresh 0000 baseline** instead of appending a `DROP TABLE localism_fund_application` migration on top of the 0001–0007 history. | The GOAL only lists the schema-file edits; the migration files still carried the table (and its name) forward forever. With no production deployment (decision 3) and local ponder DBs being throwaway docker volumes, a clean baseline removes the last localism artifact from the tree. Anyone with an old local offchain schema drops/recreates the `ponder` database once. |
| 7 | 2026-07-24 | INSTANCE_FACTORY §6.1 ("fold the accumulator address + chainid into `paramsHash` … at the next planned vkey rotation. Do not block the factory on it") | Done **in** the factory build, not after it: the trust-graph params schema goes 15 → 17 fields (`accumulator`, `chainId` appended) as the factory's first merge, rotating the trust-graph, signer and contributions vkeys before anything ships. | The research doc was written when the factory was hypothetical; the factory is precisely what manufactures the identical-clone replay hazard §6.1 describes, so shipping it first would mean rotating a schema across N live instances instead of zero. Mainnet (the decided home chain) has nothing deployed. `chainId` in the hash is also the multi-chain prerequisite §0 demands. Journal v2 is untouched — separation lives in the params, not the journal. Recorded as decided in GOAL.md's Decisions table (§8.6, delegated). |
| 8 | 2026-07-24 | INSTANCE_FACTORY §1 ("`ParamsJson.read(path, schemaUid)`" implied single override) | `ParamsJson.read` takes three deploy-supplied overrides, not one: `schemaUid`, `accumulator`, `chainId`. `params.json` keeps all three as fields, but the deploy script (and the factory, and `input-exporter`) are their source of truth. | A stale `params.json` that named another instance's accumulator would silently bind a network to a foreign domain — the exact failure mode the v2 fields exist to prevent. `input-exporter` fills both separators from the connection it is actually reading (`--accumulator`, `eth_chainId`) and **errors** on a conflicting non-zero file value rather than overriding silently. |
| 13 | 2026-07-24 | GOAL.md ground rule 5 ("frozen things stay frozen") / Decisions ("Registry changes: **zero code changes**") | Three contracts outside the factory changed, all in response to the M6 security review: (a) `EASIndexerResolver` gained a one-shot `bindSchema` and refuses attestations for any other schema; (b) `InstanceRegistry` gained `REGISTRAR_ROLE`, splitting `register` from `update`; (c) `zk_core::fixed::mul_div` and the `pagerank` rank loop now assert instead of wrapping (rotating **all four** program vkeys). | Each closed a hole that the factory turns from theoretical into routine, and none could be fixed in the factory alone. (a) The EAS binding is one-way, so any stranger could register a second schema against a live instance's resolver and fold a foreign edge into its accumulator — permanently unprovable, since the guest re-folds every leaf while `input-exporter` only collects the instance's own schema, and `acc` is a chained hash. (b) One shared role meant granting the factory `register` also granted `update`, i.e. the power to re-point any existing community's directory row — the opposite of what five places in the tree claimed. (c) `mul_div` truncated an over-256-bit quotient, so a badly-tuned instance proved WRONG scores and disagreed with the browser's arbitrary-precision port on both payouts and ranking order. The registry's "zero code changes" decision was made before the role conflation was noticed; keeping it would have meant shipping a documented security property that was false. |
| 14 | 2026-07-24 | INSTANCE_FACTORY §2.1 step 2 ("`SCHEMA_REGISTRAR.register(...)`") | `createInstance` ADOPTS a pre-existing schema UID instead of insisting on registering one, and the wizard's seed-boost slider was lowered from 20x to 4x. | The resolver address comes from plain CREATE, so it is predictable, and EAS keys schemas without `msg.sender` and reverts on duplicates. A stranger could therefore pre-register the exact tuple the factory was about to use; the revert rolled back the factory's own nonce, so the same address was predicted next time — one ~100k-gas transaction bricked `createInstance` for every creator, forever. Adoption is safe because the UID is a pure hash of `(schema, resolver, revocable)`, so a colliding UID for a freshly-created resolver can only be the record the factory wanted. The slider cap follows from the new growth bound: above roughly 4.5x, ranks grow past what U256 can hold within the wizard's own 100 iterations. |
| 12 | 2026-07-24 | INSTANCE_FACTORY §6.1 (the stated replay hazard) | **The hazard was overstated.** §6.1 says two factory clones with identical seeds/params/edge sets accept each other's proofs. On a single chain they never did: EAS derives `schemaUid = keccak256(abi.encodePacked(schema, resolver, revocable))`, so the resolver address is inside `schemaUid`, which has been a `paramsHash` field since v1. The params-schema v2 change is kept, on corrected grounds: it closes (a) cross-chain mirrors — same deployer + nonce sequence gives identical addresses on another chain (measured: identical EAS/SchemaRegistry addresses on chain 1 vs 31337) and therefore an identical v1 `paramsHash`, since nothing in the 15 fields names a chain; (b) a `setAccumulator` re-point, after which `schemaUid` names a resolver that is no longer the input source; (c) two snapshots sharing one accumulator (the contributions mirror shape). | Recorded because a security section that claims more than it delivers is worse than one that claims less — the next reader would have trusted the same-chain story. Both legs verified on the live stack via `JournalMismatch` on a real `submitProof`, including an isolation run where `schemaUid` and every knob were held constant so only `accumulator` moved. §6.1 in the research doc now carries the correction inline. |
| 9 | 2026-07-24 | INSTANCE_FACTORY §2.1 step 5 / `MerkleFundDistributor` 2-step ownership | The distributor's constructor sets `owner = owner_` **directly** instead of bootstrapping to the deployer and opening a pending transfer. `transferOwnership` after deployment is unchanged (still 2-step). Pre-approved in GOAL.md's Decisions table. | Two-step transfer protects a *live* owner from handing control to an address that cannot act; at construction there is no live owner to protect. Left as-is, `TrustgraphsFactory` would own every community's distributor until each creator remembered to call `acceptOwnership` — the opposite of ground rule 3. No live deployment is affected. Test `test_Constructor_StartsTwoStepTransferWhenOwnerDiffersFromDeployer` became `test_Constructor_SetsOwnerDirectlyWhenOwnerDiffersFromDeployer`, plus a new zero-owner rejection test. |
| 10 | 2026-07-24 | INSTANCE_FACTORY §2.1 ("3–4 CREATEs" inside `createInstance`) | The factory does **not** hold every child's creation code. `EASIndexerResolver` is created inline; `MerkleSnapshot` and `MerkleFundDistributor` are created by two thin permissionless singletons in `src/contracts/factory/InstanceDeployers.sol` that the factory calls. | EIP-170. Resolver + snapshot + distributor initcode is ~22.6 KB and lands in the factory's own *runtime* bytecode, blowing the 24,576-byte limit before any factory logic. Safe because both children take their admin/owner as constructor arguments — nothing is derived from `msg.sender` — so a deployer singleton never holds a role or ownership over what it creates. The role-free invariant test asserts this for the deployers as well as the factory. |
| 11 | 2026-07-24 | INSTANCE_FACTORY §2.2 (bounds list) | Two bounds exist that §2.2 does not mention: `admin != address(factory)` (`InvalidAdmin`) and a non-zero `EPOCH_FLOOR` at construction (`ZeroEpochFloor`). | Both are ways to *brick* an instance rather than merely misconfigure one, and both were reachable from public input. Naming the factory as admin makes step 5's grant-then-renounce act on one address, leaving a snapshot whose self-administering `CONSTITUTIONAL_ROLE` has no holder and can never be re-granted. A zero epoch floor is not "no minimum" but "no schedule": `MerkleSnapshot` treats `epochLength == 0` as unscheduled, handing epoch boundaries back to whoever calls `trigger()`. Found by the M1 test battery (`docs/build/create-a-network.md` §3). |
| 19 | 2026-07-28 | GOAL.md (proof scheduler) M3 — `submitAndClaim` as the sole payout seam, and `MerkleSnapshot`'s scope | M3's security pass (8 parallel `solidity-auditor` agents) confirmed defects that the design as written could not fix from inside the vault, so **`MerkleSnapshot` changed twice more**: it now records `checkpointRecipient[id]` on every `submitProof`, and `trigger()` refuses a checkpoint identical to the last one across BOTH lanes. `IProvingVault` also grew a standalone `claim()` and a `SubmitArgs.minPayoutUsd`. | Three findings, each defeating a headline property, and none of them addressable in `ProvingVault` alone. (a) **The seam was bypassable.** `submitProof` is permissionless, so anyone could lift a pending claim's proof out of the mempool and land it directly; the prover's `submitAndClaim` then reverted `StaleCheckpoint` and, because monotonicity is permanent, the fee became unpayable by *anyone*. The interface's headline claim ("copy a pending claim and you pay the original prover") held only for copies routed through the vault, and the cheaper copy was the one that went around it. Recording the journal's recipient makes the bounty survive the submission, so `claim()` pays it afterwards and the copier buys nothing. (b) **One proof, many checkpoints.** The journal commits the checkpoint's *contents*, not its *identity*, and two of the three shipped lane-1 seams deliberately have no `NoNewInputs` guard (`TrustAccumulatorMirror` so a quiet vouch graph cannot wedge a contributions round; `EmptyLaneAccumulator` because its lane is constant). On those, a stranger could mint an unlimited run of identical checkpoints and collect a full bounty against each. Moving the guard into `trigger()` and checking both lanes is what makes it total — `trigger()` is the only minter and the only caller that sees both lanes — and it is strictly cheaper than a fifth vkey rotation to put `checkpointId` in the journal. (c) **Every payout input was zeroable in the same block**, by front-run or by a hook re-entering from inside the forwarded `submitProof`; the checkpoint's one-shot bounty slot was consumed anyway. `minPayoutUsd` lets the prover state its price, so the community gets a reverted transaction rather than a free root. Also fixed in the same pass: the withdrawal notice debited spendable balance instantly and `cancelWithdrawal` restored it for free (a free-roots toggle, found independently by four agents); `_sizeOf`'s failed-read fallback landed on the *cheapest priced* band rather than the unpriced one, inverting the invariant its own comment asserted; `bandOf` used `max`/single-lane while the operator's cycle estimate sums both lanes, so the "same boundary" both sides test was untested for every two-lane instance; the price feed had no sanity band, so at a crashed $1/ETH a $50-capped claim withdrew 50 ETH; and `block.timestamp - updatedAt` sat in a `try` success branch where a future-dated feed would panic and block roots entirely. |
| 18 | 2026-07-28 | UPGRADE_GOVERNANCE §5.1 ("journal (10 fields, v2) … **frozen, no version byte** … versioning by fresh deployment") | The journal went to **12 fields (v3)**: `recipient` (bounty payee) and `instanceDomain` (`keccak256(abi.encode(snapshot, chainId))`, rebuilt by `submitProof` from `address(this)` + `block.chainid`). All four vkeys rotate; all three golden vector families regenerate; the frozen v1 Optimism instance is untouched and never migrated. | Both words buy something no params change can. `recipient` is what makes the bounty structurally unstealable: the fee follows the journal, so copying a pending transaction pays the original prover and refunds the copier only gas — this REPLACES `PROOF_SCHEDULER.md` §4.3's commit-reveal, which is superseded and was never built. `instanceDomain` is the universal form of `INSTANCE_FACTORY.md` §6.1: doing it in the journal instead of per-program params means no program's codec can forget it, and it closes the live hypercerts hole (issue #9) whose params carry no instance-unique field at all. **Why now rather than at some later rotation:** mainnet has nothing deployed, so it costs zero ceremony today and would be contagious across N live instances tomorrow — the same argument that moved domain separation into the factory build (entry 7). The signer program is the one asymmetry worth stating: its own seven-field `SignerJournal` stays outside the vault, so its vkey rotates purely by contagion through the shared `compute`. |
| 15 | 2026-07-28 | GOAL.md (proof scheduler) M0 item 3 ("Bind each accumulator to its snapshot", naming `AttestationAccumulator`) | The bind-once snapshot gate landed on **`EmptyLaneAccumulator` as well**, not just `AttestationAccumulator`, and both got a read-back check (`ISnapshotAccumulatorView(_snapshot).accumulator() == address(this)`) that `TrustAccumulatorMirror`'s original version does not have. | `EmptyLaneAccumulator.checkpoint()` was permissionless for exactly the same reason and with a worse consequence: on a lane-2-only instance lane 1 is constant `(0, 0)`, so the checkpoint id is the ONLY thing separating one epoch's inputs from another's. Fixing one and leaving the other would have made "a stranger cannot mint a checkpoint" (GOAL "Done when" #3) false for the program that needs it most. The read-back turns the single irreversible mistake available here — binding the wrong address, which wedges `trigger()` forever — into a revert; it also pins the ordering for a lane rotation (deploy new accumulator → `setAccumulator` → bind). Hypercerts stays out of scope as a *handler*; this is a contract fix, and the same argument that moved domain separation into the factory build applies: mainnet has nothing deployed, so it costs zero ceremony today and is contagious across N live instances tomorrow. |
| 16 | 2026-07-28 | GOAL.md (proof scheduler) M0 — journal v3 rotation scope | The **`paramsHash` zero value is now rejected** by `MerkleSnapshot`'s constructor and `setParamsHash` (`ZeroParamsHash`), which the GOAL does not mention. | `checkpointParamsHash[id] == 0` is the "not pinned" sentinel that `UnpinnedCheckpoint` reads. Without the guard, an instance deployed with a zero params hash would make every one of its checkpoints look unpinned and be unprovable in a way that reads as a contract bug rather than a misconfiguration. No real params tuple keccaks to zero, `deploy/env.ts` already refused zero, and no test constructed one, so the guard costs nothing and makes the sentinel sound rather than probabilistic. |
| 17 | 2026-07-28 | GOAL.md (proof scheduler) IF — "`input-exporter` (`--recipient`, default zero)" | The exporter also gained a **required** `--snapshot` for `GuestInput` exports (it was previously an optional lane-2 self-check flag), and `test/e2e/run.sh` + `script/DeployEasResolver.s.sol` were restructured so the snapshot exists before the first checkpoint. | `instanceDomain` is half the journal now, and it is derived from the snapshot address. An export that defaults it to zero produces a proof no snapshot can accept — a silent way to pay for a useless proof, which is precisely what ground rule 3 is about. Making it required moves that failure to export time. The e2e restructure follows from M0 rather than from choice: `resolver.checkpoint()` can no longer be called directly, and an unpinned checkpoint cannot be proven, so the minimal "EAS + resolver + schema" deploy had to grow the `resolver → schemaUid → paramsHash → snapshot → bind` chain that `DeployNetwork` already had. `DeployNetwork` also gained the `bindSchema` call it had been missing (the resolver's own docs say the factory does all three in one transaction; the script did none). A `trust-graph paramshash --params <file>` variant was added because the deploy needs the hash before any `GuestInput` can exist. |
| 20 | 2026-07-28 | GOAL.md (proof scheduler) M5 — "run the daemon unattended across multiple epochs with **one real Groth16 leg per handler** verified by the canonical SP1 gateway" | **The real-Groth16 leg was not run, and `test/e2e/fork.sh` says so in its header rather than reading as if it had been.** What the fork e2e does establish: the canonical mainnet gateway `0x397A5f7f3dBd538f23DE225B51f532c34448dA9B` is real forked state (1,975 bytes), and it REFUSES a fabricated proof when asked directly — so the mock wrap the rest of the run uses is demonstrably not something mainnet would accept. Everything above the SNARK seam is production code and is exercised end to end. | Same constraint as entry 1, restated because M5 named the leg explicitly and a silent skip would read as a pass: local Groth16 needs 16–32 GiB (this box has 11 GiB, 8 cores) and there is no prover-network key. This is the ONE done-when criterion the environment cannot supply, and it is deploy-time work, not build-time — the rehearsal belongs in the same session that first funds a prover-network key. Recorded as an explicit residual in `docs/build/run-a-prover.md` §7 so the next reader does not have to infer it from a test's absence. |
| 21 | 2026-07-28 | GOAL.md (proof scheduler) M5 — fork e2e ("Fork mainnet; stand up registry, factory, vault; run the daemon unattended across multiple epochs") | `test/e2e/fork.sh` forks mainnet, takes its two fork-dependent assertions, then **DETACHES**: it dumps state, reloads with no upstream, and re-installs the canonical gateway's bytecode with `anvil_setCode`. The multi-epoch run and the whole adversarial pass execute against that chain (still chain id 1) rather than a live fork. | Not a preference — a live fork cannot survive a run this long. anvil re-reads the EIP-2935 block-hash-history contract from the upstream on **every block it mines** and treats any failure there as fatal, panicking mid-block with `pre-execution changes failed`. Four attempts died that way at four different points across two providers (a 403 from an archive-gated endpoint, a 429, and a dropped connection). `anvil_dumpState` carries only anvil's own accounts, never lazily-fetched remote ones, so keeping mainnet state wholesale is not on the table either. The honest split is therefore: everything that genuinely needs the real chain runs against the real fork **before the first block is mined** (both assertions are pure `eth_getCode`/`eth_call`), and everything else runs deterministically offline against the one piece of mainnet it depends on. The script probes candidate RPCs for archive-state service before forking, because a provider that answers `eth_blockNumber` and refuses archive reads fails as an anvil panic that looks like our bug. |
| 22 | 2026-07-28 | GOAL.md (proof scheduler) M2 — "the daemon enumerates the registry every tick" | The registry scan gained a **`registry_from_block` config key** and became **incremental**, and the catalog reader is now built **once per tick instead of once per program**. Found by running the daemon against a mainnet fork; not in the GOAL. | Two defects, both invisible on a fresh anvil and both fatal on a real chain. (a) The `InstanceRegistered` scan started at block 0. Against a registry deployed at block 21,000,000 that is ~2,100 empty `eth_getLogs` calls per pass — and most public providers reject the range outright as an archive request, so the daemon did not run slowly, it got **no catalog at all and every tick failed**. That is exactly what happened on the first fork run. (b) It rescanned the full history once per supported program, so three programs meant three identical scans a minute for a list that grows by a row a week. Registrations are append-only and a scanned block never changes its logs, so the history is now built once and extended from `scanned_to + 1`. Deliberately NOT rewound on a reorg: a re-orged-out registration leaves a stale entry that degrades to a per-instance `ReadFailed` skip and self-heals, whereas dropping entries newer than `head - confirmations` would delete real ones every time the chain merely paused. Startup alerts when `registry_from_block` is 0 on any chain that is not 31337. Range arithmetic is unit-tested (`chain::scan_range_tests`, 6 cases including the no-gap invariant). |
| 23 | 2026-07-28 | GOAL.md (proof scheduler) M5 — "one real Groth16 leg per handler: a curated trust-graph instance, a vault-funded trust-graph instance, a **contributions round**, and a **signer-sync rotation**" | `test/e2e/fork.sh` drives **two** of the four: a vault-funded trust-graph instance and a curated one, both in the same loop, across three epochs. The contributions and signer-sync legs are **not** driven by the daemon anywhere; they remain covered by `test/e2e/run.sh`, which proves each program's full pipeline on anvil through the CLI a human drives. | Cost, and being straight about what it buys. A contributions round needs its own snapshot, a `TrustAccumulatorMirror`, a params file, an IPFS daemon, and a seeded six-persona claim/rating set (`taskfile/contributions.yml`); a signer-sync rotation needs a Safe with the Zodiac module wired. Both are per-instance *setup*, and neither exercises a daemon code path the trust-graph legs miss — the handler dispatch, the journal, the finality guard, the vault seam and every decision branch are program-agnostic, and the per-program difference is confined to `handlers.rs` input reconstruction, which `run.sh` covers directly for all four programs. What is genuinely NOT shown is the daemon *scheduling* those two programs end to end, which is a real gap and is listed as one in `docs/build/run-a-prover.md` §7 rather than implied away. The two legs that were built are the two the milestone is actually about: they are the free tier and the paid tier, and they are the only pair that can disagree with each other (an earlier bug had the operator treat every non-vault instance as unfunded and refuse to work at all — caught precisely because both run in one loop). |
| 24 | 2026-07-28 | GOAL.md (proof scheduler) "Done when" #4 — "unpreventable spend is budgeted: the loss budget halts an instance rather than bleeding" | **The loss budget was unreachable code and the criterion was false.** `LossBudget::exceeded_by` was implemented and unit-tested in `operator-core`, but the daemon called `plan(&state, &policy, Spend::default())` on every instance on every tick, so no input could ever trip it. Closed in M5: `Record::Intent` gained `cost_cents`, `Journal::spend()` sums a rolling window, and the daemon passes the real number. `[budget]` gained `cents_per_billion_cycles` and `window_seconds`. | An M2 `TODO` that read as a deferral ("lands with the loss-budget work") but was the last piece of it — everything else already existed. Caught by walking the done-when list against the code rather than against the tests, which is exactly the failure mode a green suite hides: 34 `decide` tests exercised the budget through `plan`, and every one of them passed a `Spend` the daemon never constructed. Three choices worth stating. Cost is recorded **at intent time**, because it depends on the instance's size then and by the next tick the graph has moved. Spend counts **intents, not settlements**: a request that failed still cost money, and counting settlements would let a run of failures spend without ever registering. And `cost_cents` is `serde(default)`, so a journal written before the field replays at zero — the safe direction, since a budget must not halt an instance on a number nobody recorded. Now exercised end to end in `test/e2e/fork.sh` (cap set below the day's spend, instance halts, zero further requests) plus four journal tests. |
| 5 | 2026-07-23 | GOAL.md (contributions) M1 property list / CONTRIBUTION_FUNDING §5.1 | The padding property is stated as "contributor-list padding never increases any outsider's payout"; the M1 fuzz suite produced a counterexample to every set-level formulation of it and it was replaced by two provable properties: (a) padding a fully-consented claim never increases the padded set's combined take, and (b) with burned consent mass (rejected/unaccepted shares) an accepting padded-in address can recapture value, but the set's take never exceeds the claim's **full-consent ceiling**. §5.1 of the research doc now carries the sharpened statement. | Under relative distribution, shrinking one claim's realized consent mass necessarily grows every other claim's take (and vice versa), so "no outsider payout increases" is unattainable as stated. The recapture path is not a mint — it is bounded by the value raters assigned to the claim — and rejection still strictly reduces the claim's take versus consenting. Tests: `padding_a_fully_consented_claim_never_mints`, `padding_recapture_never_exceeds_the_full_consent_ceiling`. |
| 25 | 2026-08-19 | GOAL.md S0 / BUZZ_NOSTR_PLAN §11–§12 (pinned Buzz fixture and no upstream dependency) | The supported Buzz profile is the immutable upstream commit `a362fecc2389955f942c9581bdfeba379ab115b3` **plus** the checked-in compatibility patch `test/fixtures/nostr/buzz/a362fecc2389955f942c9581bdfeba379ab115b3/buzz-trustgraphs-compat.patch` (SHA-256 `3129e43e7b8967635bde8dd4a084613ef8628146dd1d1ba2f62e41ced4762a62`). The patch registers addressable kinds 36382/36383/36384, admits them with explicit global routing/scopes, and admits the already-defined 43001–43006 job kinds with required channel routing. Pilot source identity is the ordered pair `(upstream SHA, patch digest)`, never an unlabelled fork or moving branch. | Source inspection found that the pinned commit's closed ingest allowlist rejects all unknown kinds and also omits its own job constants 43001–43006. Therefore an unmodified deployment cannot contain the S0-required V1, J1, binding, or Option-C head events even though the design assumed it could. A tiny reviewable patch is the only way to exercise those signed events through the real relay without making an upstream merge load-bearing. The proof format remains independent of Buzz's allowlist; unpatched deployments may still prove supported G1/F1 history but cannot claim full v1 compatibility. |
| 26 | 2026-08-19 | GOAL.md S0 benchmark gate / BUZZ_NOSTR_PLAN §§5, 8, 11 / `research/nostr` spike | The 2026-08-16 Nostr-specific SP1 numbers are retired and may not set caps or pricing. Correct guest verification uses `k256::schnorr::signature::hazmat::PrehashVerifier::verify_prehash(event_id, signature)`; both benchmark bins were corrected and require a fresh marginal plus whole-envelope run. | The old bins imported message-level `Verifier`, which SHA-256s its input before BIP-340 verification. Rust-nostr/libsecp already signs the SHA-256 NIP-01 event id as the 32-byte message. The deterministic corpus independently proves the mismatch: the real signature passes `verify_prehash(event_id, sig)` and fails `verify(event_id, sig)`. Thus both the 31,747-cycle signature row and the 48,858-cycle full-event row measured a different signature contract; adjusting them by a hash estimate would not satisfy S0's measured-evidence gate. |
| 27 | 2026-08-19 | GOAL.md S0 serializer/live-fixture gate / BUZZ_NOSTR_PLAN §11 | The every-control-byte case is classified as a serializer-only source vector rather than a stored/audited live event. The checked-in live fixture instead records the relay's HTTP 500 probe and keeps all valid stored inputs byte-identical; its authoritative DB export has 35 signed rows, a complete 30-row audit prefix, and 5 direct rows. | Rust-nostr correctly signs and serializes U+0000 as `\u0000`, but Buzz stores tags in PostgreSQL `jsonb`, whose input parser rejects that escape (`unsupported Unicode escape sequence`). Treating the hand-built source row as live would make the fixture agree with an impossible database state. The independent verifier still checks every control byte at the NIP-01 layer. The live run also exposed four generated kind-40099/44100 notification rows that, like the `buzz-admin` roster, bypass `EventCreated`; completeness is therefore defined by the explicit DB-minus-audit direct-row set, not by assuming roster is the only exception. |
| 28 | 2026-08-19 | GOAL.md S0 measured caps / fixture ADR's provisional 16-MiB TGNW maximum | The hard TGNW envelope cap is reduced to **12 MiB (12,582,912 bytes)**. The joint hard limits estimate to 826.9M PGU under the frozen conservative formula; the pilot is stricter at 4 MiB, 2,048 audit entries, 128 OA checks, and 400M estimated PGU. Corrected marginals are 70,149 PGU/NIP-01 check, 11,920/audit entry, and 61,246/OA check; the final 12-MiB-cap build executes the 20,297-byte live envelope at 3,631,054 PGU. | The source-only 16-MiB number predated the required SP1 measurement. Charging each byte twice at the measured SHA-256 rate and then applying the 2× decode/state/variance factor puts 16 MiB plus the other hard maxima just above the network's 1B-PGU default. Twelve MiB leaves about 17% headroom while the 4-MiB pilot cap leaves substantial room for S2 graph semantics. Keeping the unmeasured 16-MiB value would violate the S0 gate rather than preserve a meaningful compatibility promise. |
| 29 | 2026-08-19 | GOAL.md outcome #5 (second epoch exercises "carry/drop behavior") / S4 two-epoch e2e | The live S4 epoch exercises a withheld newest Option-C head as `DROPPED`; it does **not** manufacture a `CARRIED` on-chain state. The core retains a regression for `CARRIED` on a same-maximum-count malformed/unavailable claim, but that shape is unreachable through the current `AnchorRegistry`, which requires every node's count to increase strictly. | H-5 deliberately forbids falling back to any lower signed count, while `AnchorRegistry.anchor` deliberately rejects equal counts for every node kind. Together those safety rules mean a live newer unavailable head must drop, and the only core shape that carries (an earlier valid claim at the same maximum count) cannot pass ingress. Weakening either rule solely to make the rehearsal display `CARRIED` would re-open stale resurrection or let an admitted relayer consume finite capacity without advancing a head. The program-level case stays tested as defensive/backward-compatible semantics; production documentation states the reachable behavior plainly. |
| 30 | 2026-08-19 | GOAL.md S4/Done #6 (Nostr output "consumable by trust-compose") | S4 ships **authenticated trust-compose capture**, not score consumption: `CompositionSourceAdapter` captures the exact Nostr program, vkey, params, verifier provenance, checkpoint, and unified root, while trust-compose v1 does not ingest the Nostr bytes32-keyed score blob. Frontend source selection also refuses to present it as an address-keyed v1 composition source. | The accepted S4 bullet is specifically capture “without domain confusion,” but the broad done sentence says consumable. Trust-compose v1's guest accepts canonical address-keyed distributions; silently projecting or relabeling the Nostr member domain would make the broader sentence true by breaking the narrower safety requirement. A future reviewed projection/v2 can consume bound-member address leaves with an explicit output domain and completeness statement. Until then capture preserves provenance and fails closed instead of pretending bytes32 Nostr nodes are addresses. |
| 31 | 2026-08-20 | Network-creation GOAL.md clarification 9 / `docs/build/contributions/interfaces.md` §3–§4 (frozen params + journal) | The contributions params layout stays at 21 slots with **journal-level domain separation only**: no `chainId`/`accumulator` fields are added to the tuple when `ContributionsFactory` arms the program for permissionless-shaped creation. Replay separation remains the journal's `instanceDomain = keccak256(abi.encode(snapshot, chainId))`, rebuilt on-chain by `submitProof`. | Ruled in the network-creation GOAL (Jake, 2026-08-19): the journal domain already binds each proof to one snapshot on one chain, so v2-style tuple fields would add no security — and adding them would regenerate `test/golden/contributions.json` and rotate the contributions vkey, which this goal forbids (existing vkeys stay byte-identical). The door stays open for a future codec bump; this row is the visible record that the omission is a decision, not an oversight. |
| 32 | 2026-08-20 | Network-creation GOAL.md M6 / `DeployContributionsInstance.s.sol` conventions | `ContributionsFactory` changes three script-era conventions: `instanceId = keccak256(abi.encode(creator, name, salt))` (the script mixed in the snapshot address, which is not pre-computable); the round's distributor is created with **fee 0 and `feeRecipient = admin`** (the script hard-coded 3% to the deployer); and the per-instance verifier + TestUSDC + params-file write-back are gone (shared vkey-cross-checked verifier; the daemon serializes params from chain). The frozen interface file is untouched — schema strings, 21-slot layout, journal mapping, and golden vectors are byte-identical — and the new `ContributionsInstanceCreated` event is purely additive next to the registry events `operator-core` reconstructs from. | The factory path must satisfy the wizard pattern (pre-computable ids), ground rule 3 (no value routed to a deployer key), and the shared-singleton verifier discipline of the other program factories. Recorded here because the script's outputs were the program's de-facto creation interface even though the normative interface doc never froze them. |

| 33 | 2026-08-20 | Network-creation GOAL.md clarification 7 / `ProvingVault.bandOf` (research/PROVING_VAULT.md pricing table) | `bandOf` gains a `trust-graph-weighted` arm priced with sized bands exactly like `trust-graph`, and the governed wrappers derive their guardrail fee band from the vault instead of a hardcoded literal (the original `GovernedTrustgraphsFactory` hardcoded band 1, which is wrong for flat-banded programs such as trust-compose). Dev vault redeployed by the normal pipeline; no production vault exists. | The weighted guest proves the same fixed-point PageRank over the same accumulator-committed edge log (plus a creation-bounded prior manifest), so the on-chain counters are the work and sized bands are the honest price. Without the arm the program answered 0 (unpriced) and every governed-weighted prepay path was dead on arrival; with a literal instead of a vault read, each new wrapper would silently re-introduce the wrong band for its program. |
| 34 | 2026-08-20 | Network-creation GOAL.md clarifications 1/6, invariant 2 / `MerkleGovModule` constructor events | `MerkleGovModule` construction is now SILENT: `_setMerkleSnapshotContract` no longer emits during construction/setUp, and a one-shot permissionless `publishInitialSnapshotBinding()` (mirroring the typed controllers' `publishInitialVersion()`) emits `MerkleSnapshotContractUpdated(0, snapshot)` after the creating factory's discovery event. A snapshot rotation consumes the one-shot and announces itself. The module is deployed through a new shared `MerkleGovModuleDeployer` used by all three governed wrappers (and the rebased `GovernedTrustgraphsFactory`, a dev-only redeploy — no production deployment exists). | The constructor event preceded `GovernedInstanceCreated` inside the creating block, and Ponder's block-granular factory matching dispatched it before the row-inserting discovery handler — the exact ordering that permanently wedged the indexer on wizard-created networks. Discovery-before-children (invariant 2) is now enforced at the source for new deployments while the indexer-side ensure pattern heals existing chains. The deployer also removes the module initcode from each wrapper's EIP-170 budget, which is what makes three governed wrappers fit. |

**2026-08-14 follow-up to #23:** issue #21 supersedes the signer half of that residual. Governed
creation now emits a complete signer descriptor, the daemon discovers and schedules it without a
manifest, signer proofs have distinct finality/loss budgets, and a real Safe integration applies
the resulting owner set. The Contributions unattended leg remains outstanding exactly as #23 says.

Notes that are **not** deviations, recorded for context:

- Executor-only prover commands (`execute`, `vkey`, `paramshash`) default to `SP1_PROVER=mock` in
  `taskfile/zk.yml` and `test/e2e/run.sh`: the default cpu backend eagerly allocates the ~5 GiB CPU
  prover machine these commands never use, OOM-killing small boxes/CI runners. The executor and the
  guest==native byte-assert are identical under mock; `prove` respects the caller's `SP1_PROVER`.
- The platform reorg (M0) re-derived vkeys (ELF layout changed under the refactor; semantics didn't —
  golden vectors are byte-identical to pre-reorg). Recorded in [`docs/concepts/networks-and-programs.md`](../docs/concepts/networks-and-programs.md);
  rotation of the live stack was batched to the M2 deploy per that plan's ground rule against
  dribbling vkey rotations.
- Contributions slot-A wiring is a **read-only mirror**, resolving CONTRIBUTION_FUNDING.md §3's
  stated open question ("whether the contrib snapshot reads the trust accumulator's existing
  checkpoints or pushes its own — two snapshots triggering one accumulator needs a look"): the
  contrib `MerkleSnapshot`'s `accumulator` is a `TrustAccumulatorMirror` whose `checkpoint()`
  READS the trust accumulator's live `(acc, leafCount)` and freezes it into the mirror's own
  checkpoint array — nothing is ever pushed into the trust accumulator, so the two instances'
  trigger cadences cannot race and the trust instance's checkpoint ids stay its own. The mirror
  deliberately has NO `NoNewInputs` guard (a quiet vouch graph must not wedge contrib round
  triggers; spam is bounded by the contrib snapshot's `epochLength` gate). The proven input
  commitment is unchanged — `acc` already commits to the full ordered edge log.
- The contributions schema strings are REGISTERED in the canonical comma-no-space form
  (`string title,bytes32 contentHash,…` — the house schema-string format every existing network
  entry uses and the config field parser consumes); `docs/build/contributions/interfaces.md` §1's
  comma-space rendering is table presentation, not wire format. The golden vectors bind schema
  UIDs as opaque placeholders, so nothing golden depends on the string spelling.
